<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:yourapp="http://schemas.android.com/apk/res-auto" >
    <!-- Search, should appear as action button -->
    <item android:id="@+id/action_voice_note"
        android:icon="@drawable/abc_ic_voice_search"
        android:title="@string/menu_voice_record_button"
        yourapp:showAsAction="ifRoom"  />
    <item android:id="@+id/action_take_picture"
        android:icon="@android:drawable/ic_menu_camera"
        android:title="@string/menu_add_picture_button"
        yourapp:showAsAction="ifRoom"  />
    <item android:id="@+id/action_set_sensitivity"
        android:icon="@android:drawable/ic_menu_edit"
        android:title="@string/menu_set_sensitivity"
        yourapp:showAsAction="ifRoom"  />
    ...
</menu>